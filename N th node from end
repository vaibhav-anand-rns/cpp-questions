struct node{
    
    int data;
    node->next;
    node(int val){
        
        data = val;
        next = nullptr;
    }
}
node* getNthFromEnd(node* head, int n){
    
    node*first = head;
    node*second = head;
    
    for(int i = 0; i<n; i++){
        
        if(first == nullptr){
            
            return nullptr
        }
        first = first->next;
    }
    
    while(first != nullptr){
        
        first = first -> next;
        second = second -> next;
    }
    return second;
   
}
