#include <bits/stdc++.h>
using namespace std;

void permute(string s, int l, int r){
    if(l == r){
        cout << s << endl;
        return;
    }
    for(int i = 1; i<n; i++){
        swap(s[l],s[i]);
        permute(s,l+1,r);
        swap(s[l],s[i]);
    }
    
}
